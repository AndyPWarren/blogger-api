'use strict';

/**
 * A group represents a shared asset which other data can be associated with. \
 * A user must have a membership with a group to enable interaction with associated data.
 *
 * API docs: http://docs.firstmate.apiary.io/#reference/group
 * @module GroupController
 */
var SiteController = {

};

/**
 * @property exports
 * @type {Object}
 */
module.exports = SiteController;
